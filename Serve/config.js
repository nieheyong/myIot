/**
 * config
 */


var config = {
    // debug 为 true 时，用于本地调试
    debug: true,

    host: 'localhost',
    // mongodb 配置
    mongodb: 'mongodb://127.0.0.1/myIot',
    port: 3000
};

module.exports = config;
